apiVersion: v1
kind: Service
metadata:
  name: {{ include "uvoo-minicms.fullname" . }}
  labels:
    {{- include "uvoo-minicms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uvoo-minicms.selectorLabels" . | nindent 4 }}
